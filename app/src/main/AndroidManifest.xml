<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_simaksi_launch"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_simaksi_launch"
        android:supportsRtl="true"
        android:theme="@style/Theme.ESIMAKSI">
        <activity
            android:name="dashboard.MainActivity"
            android:exported="false" />
        <activity
            android:name="FITURLOGIN.Register2Activity"
            android:exported="false" />
        <activity
            android:name="FITURLOGIN.Register1Activity"
            android:exported="false" />
        <activity
            android:name="FITURLOGIN.TampilanPertamaActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="FITURLOGIN.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>